server {
    listen 80;
    server_name localhost;

    location /api/ {
        proxy_pass http://backend:50501/;
    }
    
    location / {
        root /usr/share/nginx/html;
        try_files $uri /index.html;
    }
}